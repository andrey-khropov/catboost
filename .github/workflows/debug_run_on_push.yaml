on:
  push:
    branches:
      - github_actions_debug

jobs:
  check:
    name: Check per OS
    strategy:
      matrix:
        os: [ubuntu-latest, macos-13, windows-2022]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - name: check chmod
        shell: pwsh
        run: |
          New-Item -Path . -Name "script.py" -ItemType "file"
          if ("${{ matrix.os }}" -ne "windows-2022") {
            echo "Not on Windows"
            chmod a+x script.py
          } else {
            echo "On Windows"
          }

  test:
    name: Test per OS
    strategy:
      matrix:
        os: [ubuntu-latest, macos-13, windows-2022]
      fail-fast: false
    uses: ./.github/workflows/test.yaml
    with:
      runs-on_value: ${{ matrix.os }}
      artifacts_repository: catboost/catboost
      artifacts_version: f739ba42da12adf1467c4980f7f6d8fc5a904d05 
      artifacts_run_id: 9734005339
      catboost_package_version: 1.2.5